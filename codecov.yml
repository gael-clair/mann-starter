# Default Codecov used if no codecov.yml found
# codecov:
#   require_ci_to_pass: yes

# coverage:
#   precision: 2
#   round: down
#   range: "70...100"

# parsers:
#   gcov:
#     branch_detection:
#       conditional: yes
#       loop: yes
#       method: no
#       macro: no

# comment:
#   layout: "reach,diff,flags,tree"
#   behavior: default
#   require_changes: no

coverage:
  status:
    project:
      default: false
      unit-tests:
        target: 80%
        flags: unit
      e2e-tests:
        target: 65%
        flags: e2e
    patch: false

parsers:
  javascript:
    enable_partials: yes
